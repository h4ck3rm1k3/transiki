<%= form_for(@mediawikicategory) do |f| %>
  <% if @mediawikicategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mediawikicategory.errors.count, "error") %> prohibited this mediawikicategory from being saved:</h2>

      <ul>
      <% @mediawikicategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mediawikiserver_id %><br />
    <%= f.text_field :mediawikiserver_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
