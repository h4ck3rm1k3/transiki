<%= form_for(@route_point) do |f| %>
  <% if @route_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route_point.errors.count, "error") %> prohibited this route_point from being saved:</h2>

      <ul>
      <% @route_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">  <%= f.label :point_id %><br />    <%= f.text_field :point_id %>
  <div class="field">  <%= f.label :route_id %><br />    <%= f.text_field :route_id %>
  <div class="field">  <%= f.label :version %><br />    <%= f.text_field :version %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
